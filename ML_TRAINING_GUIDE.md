# ML模型训练指南

## 当前状态

**数据库中的数据**:
- 快照数量: 1
- 不足以训练ML模型（需要至少50个样本）

**原因**: 数据库存储今天才开始启用，需要积累历史数据

---

## 解决方案

### 方案1: 继续使用演示模型（推荐现在使用）

演示模型已创建，可以正常使用ML功能：
- 准确率: 65%
- 功能完整: 可以提供上涨概率预测
- 文件位置: `output/stock_model.pkl`

**优点**: 立即可用，无需等待

### 方案2: 积累真实数据后训练（推荐长期使用）

#### 步骤1: 运行采集服务积累数据

```bash
# 运行采集服务（需要持续运行几天）
python collector_service.py

# 或者手动定期采集
python fetch_hot_stocks.py
```

#### 步骤2: 等待数据积累

- 最少需要: 50个时间点的快照
- 建议: 至少1-2周的数据
- 理想: 1个月以上的数据

#### 步骤3: 构建训练数据

```bash
python build_training_data.py
```

#### 步骤4: 训练模型

```bash
python train_model.py
```

---

## 数据需求说明

| 数据量 | 效果 | 建议 |
|--------|------|------|
| < 50 | 无法训练 | 继续使用演示模型 |
| 50-200 | 基础模型 | 可以训练，但准确率有限 |
| 200-1000 | 较好 | 可以训练出不错的模型 |
| > 1000 | 理想 | 可以训练高质量模型 |

---

## 演示模型 vs 真实模型

| 特性 | 演示模型 | 真实模型 |
|------|----------|----------|
| 立即可用 | ✅ | ❌ (需积累数据) |
| 基于真实市场数据 | ❌ | ✅ |
| 预测准确率 | ~65% | 可能更高 |
| 功能完整性 | ✅ | ✅ |

**建议**: 
- 现在: 使用演示模型体验完整功能
- 未来: 积累数据后训练真实模型替换

---

## 当前系统状态

✅ **已完成**:
- 演示模型已创建并加载
- ML功能已启用
- 页面显示"规则模型 + ML模型（已启用）"

**无需额外操作，系统已可正常使用！**
